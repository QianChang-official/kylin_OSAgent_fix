# Security Policy

## 支持范围

安全修复只应用于最新次版本。部署方应固定到确切版本，升级前先看 CHANGELOG。

## 报告漏洞

请使用本仓库的 GitHub private vulnerability reporting，不要开公开 issue。
报告中请勿包含真实凭据、生产审计记录、客户日志或真实目标 IP。

## 信任边界

理解这些边界，才能判断一次告警是不是真的越界。

### 系统假定不可信的

- **模型输出**。模型只做意图理解与工具规划，规划结果必须通过工具白名单与参数
  校验才可能执行。模型永远不能直接下发系统命令。
- **用户自然语言输入**。Guardrail 在**调用模型之前**做本地预检，高危请求在此
  终止，不进入模型上下文。
- **工具输出**。工具返回内容会做二次安全复检，防止把危险内容当作可信数据回灌。
- **控制台前端**。前端不是安全边界。口令校验、会话签名、CSRF、限流全部在服务端。

### 系统依赖其为可信的

- **宿主机 root 与文件系统**。能写 `data/audit.db` 的人可以改审计；能改
  `backend/` 的人可以改掉护栏本身。本项目不防御已取得宿主 root 的攻击者。
- **`AUDIT_HMAC_KEY` 的保管**。见下节。
- **`COMMAND_WHITELIST` 的正确性**。白名单里放进危险命令，护栏不会替你兜底。

## 审计的可信度：能证明什么，不能证明什么

审计链把"审计不可篡改"从声明变成可机器验证的性质，但两个性质必须分开看：

| | 机制 | 能证明 | **不能**证明 |
| --- | --- | --- | --- |
| 完整性 | SHA-256 哈希链 | 记录被改写、删除、重排，或整张日表丢失，且能定位到具体行 | 有数据库写权限者整体重建一条自洽的链 |
| 真实性 | `AUDIT_HMAC_KEY` 的 HMAC-SHA256 签名 | 上述整体重建不可行 | 持有密钥者的伪造 |

**未配置 `AUDIT_HMAC_KEY` 时，审计只有完整性，没有真实性。** 一个能写数据库的
攻击者可以改一条记录后重算整条链，验证器会报告 `integrity OK`。这不是缺陷，是
无密钥哈希链的固有性质，并且由 `test_the_same_forgery_is_invisible_without_a_key`
固定下来，防止文档过度宣称。

要获得真实性，请配置 `AUDIT_HMAC_KEY`，**并把密钥放在被审计主机之外**。密钥与
审计库在同一台机器上，只能提高伪造成本，不能消除伪造可能。

离线核验（不需要后端在跑）：

```bash
python scripts/verify_audit_chain.py --db data/audit.db
AUDIT_HMAC_KEY=... python scripts/verify_audit_chain.py --db data/audit.db --json
```

破损时非零退出并给出首个断点的表、行号与 `request_id`。

## 执行边界

- 所有系统命令经 `backend/executor/safe_executor.py` 单点收口，`shell=False`，
  参数化调用。这一点由 INV-S1..INV-S5 的 AST 静态检查在 CI 中强制。
- 自动执行范围仅限**只读**运维工具，以及不修改文件的清理扫描/计划。
- 中风险工具调用需要 dry-run 加一次性令牌人工确认。
- 可恢复清理只在临时目录白名单内工作，做隔离与恢复，不永久删除。
- 审计库、溯源证据、控制台构建产物在任何白名单校验**之前**即被拒绝，自动清理
  永不可达（INV-R7）。

## fail-closed 的范围

审计不可写时，`/chat`、`/tools/call`、`/tools/confirm` 以及经 `mcp_adapter`
委派的两种 MCP 传输都会拒绝执行并返回 503。

**边界**：预检在工具执行**之前**，能挡住"审计已经不可写却仍去执行"。但工具执行
完成之后才发生的审计写失败无法撤销已发生的动作——此时 `AuditWriteError` 会向上
抛出，请求以错误结束而不是伪装成功。这是这道门能关上的部分，不是全部。

## 前门欺骗的定位

公开登录页的蜜罐是**欺骗手段，不是安全边界**。边界在后端：PBKDF2 口令校验、
HMAC 签名会话、CSRF、独立限流。蜜罐只做一件事——让自动化爆破打空，并在打空过程
中留下证据。沙箱令牌与真实令牌由同一会话密钥派生的**不同子密钥**签名，互相验证
均失败（INV-R6）。

## 已知限制

诚实列出，以免被读成比实际更强的保证：

- Guardrail 是规则与风险评分的组合，不是形式化证明。安全对抗基准是 63 条合成
  用例，覆盖已知攻击模式，不等价于对未知攻击的保证。
- 无 `AUDIT_HMAC_KEY` 时审计不具备真实性（见上）。
- 溯源证据 `incidents.jsonl` 目前是追加写入，尚未纳入哈希链。
- 不防御已取得宿主 root 的攻击者。
- 离线安全规划器在无外网时保证闭环可用，但规划质量低于在线模型。

## 密钥管理

API Key 与 `AUDIT_HMAC_KEY` 只能经环境变量注入，不写入代码、文档、日志或数据库。
CI 中有针对常见密钥格式的扫描步骤，提交即失败。
