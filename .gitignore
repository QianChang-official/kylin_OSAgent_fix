# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
.venv-frontend/
venv/

# Node
node_modules/
.vite/
safeopsagent/.playwright-cli/
safeopsagent/output/

# Runtime data — audit / metrics / baselines are generated at run time
safeopsagent/data/
*.db
*.sqlite3

# Secrets — only .env.example is tracked
.env
*.key
*.pem

# Build & packaging artifacts
*.tar.gz
*.tar.gz.sha256

# Editors / OS
.idea/
.vscode/
.DS_Store

# Local working notes, not part of the delivery.
# Anchored to the repo root on purpose: a bare *.html would also swallow
# backend/static/console/index.html and the vite entry point.
.workbuddy/
/上下文压缩.txt
/SafeOpsAgent*.html
/safeopsagent.zip
